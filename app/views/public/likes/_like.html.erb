<div class="container">
  <div class="row">
    <div class="col">
      <div class="comment-all d-flex justify-content-around">
        <p>いいね件数:<span class="text-success ml-2"><%= @post.likes.count %></span></hp>
        <% if @user.already_liked?(@post) %>
          <div class="like-delete ml-4">
            <%= link_to  public_user_post_like_path(@user.id,post_id: @post.id, id: @post.likes[0].id),class: "liked text-danger", method: :delete, remote: true do %>
              <p>いいねを取り消す♡</p>
            <% end %>
          </div>
        <% else %>
        <div class="like-create ml-4">
          <%= link_to  public_user_post_likes_path(@user.id,@post.id), method: :post, remote: true do %>
            <p>いいねする❤</p>️
          <% end %>
        </div>
        <% end %>
        <div class="flex-path">
        <h6 class="text-secondary ml-4">
          <%= link_to "いいねしたユーザー", public_user_post_likes_path(@user.id,@post.id), class: "btn btn-success"%> </h6>
        </div>
        </div>
      </div>
    </div>
  </div>
</div>

<s
